import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Trending",
};

export default function Page() {
  return (
    <div className="p-8 text-text-secondary">
      Page: /trending — replacement pending
    </div>
  );
}
