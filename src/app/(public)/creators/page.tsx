import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Creators",
};

export default function Page() {
  return (
    <div className="p-8 text-text-secondary">
      Page: /creators — replacement pending
    </div>
  );
}
