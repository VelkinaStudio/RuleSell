import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Ruleset",
};

export default function Page() {
  return (
    <div className="p-8 text-text-secondary">
      Page: /r/[slug] — replacement pending
    </div>
  );
}
