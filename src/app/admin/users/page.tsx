import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Admin: Users",
};

export default function Page() {
  return (
    <div className="p-8 text-text-secondary">
      Page: /admin/users — replacement pending
    </div>
  );
}
