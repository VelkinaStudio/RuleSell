import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Admin: Reports",
};

export default function Page() {
  return (
    <div className="p-8 text-text-secondary">
      Page: /admin/reports — replacement pending
    </div>
  );
}
