import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Admin: Rulesets",
};

export default function Page() {
  return (
    <div className="p-8 text-text-secondary">
      Page: /admin/rulesets — replacement pending
    </div>
  );
}
