import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Billing settings",
};

export default function Page() {
  return (
    <div className="p-8 text-text-secondary">
      Page: /settings/billing — replacement pending
    </div>
  );
}
