import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Profile settings",
};

export default function Page() {
  return (
    <div className="p-8 text-text-secondary">
      Page: /settings/profile — replacement pending
    </div>
  );
}
