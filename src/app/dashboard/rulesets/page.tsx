import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "My rulesets",
};

export default function Page() {
  return (
    <div className="p-8 text-text-secondary">
      Page: /dashboard/rulesets — replacement pending
    </div>
  );
}
