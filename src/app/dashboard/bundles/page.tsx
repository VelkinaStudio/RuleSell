import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "My bundles",
};

export default function Page() {
  return (
    <div className="p-8 text-text-secondary">
      Page: /dashboard/bundles — replacement pending
    </div>
  );
}
