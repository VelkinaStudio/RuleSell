import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Earnings",
};

export default function Page() {
  return (
    <div className="p-8 text-text-secondary">
      Page: /dashboard/earnings — replacement pending
    </div>
  );
}
