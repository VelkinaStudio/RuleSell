import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Purchases",
};

export default function Page() {
  return (
    <div className="p-8 text-text-secondary">
      Page: /dashboard/purchases — replacement pending
    </div>
  );
}
